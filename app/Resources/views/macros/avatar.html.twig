{% macro avatar(user) %}
    {% if user.avatar is not null %}
        <img src="{{ asset('avatars/' ~ user.id ~ '.' ~ user.avatar ) }}" alt="" />
    {% else %}
        <img src="{{ asset('avatars/default_avatar.png' ) }}" alt="" />
    {% endif %}
{% endmacro %}